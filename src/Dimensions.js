
 class Dimensions  {
    constructor (
        tabletopLength,
        tabletopWidth,
        tabletopHeigth,
        legHeight,
        legWidth,
        apronHeight,
        apronWidth,
        apronLength,
    ) {
     this.length = length;
     this.width = width;
     this.height = height;
      }
}